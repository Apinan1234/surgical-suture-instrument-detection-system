# PROJECT-COMPLETION-CHECKLIST — สถานะทั้งโปรเจกต์ (15 ส.ค. 2569)

เช็คก่อนเริ่มทำสไลด์ PowerPoint ต่อ — ตรวจจากไฟล์จริงทุกข้อ (เปิด `.docx` แกะข้อความ, เทียบตัวเลขกับ
`model-results-2026-08-13.md`, อ่าน handoff ล่าสุด) ไม่ได้เดาจากชื่อไฟล์หรือ commit message

**สัญลักษณ์:** ✅ เสร็จแล้ว · ⚠️ ทำบางส่วน/ต้องตรวจซ้ำ · ❌ ยังไม่ทำ · ⏸ เลื่อนไว้โดยตั้งใจ

---

## 🔴 สิ่งที่สำคัญที่สุดก่อนอื่น — รายงานกับผลจริงไม่ตรงกัน

**บทที่ 4-5 ของ `05_reports/แก้3-fixed.docx` ยังพูดถึงผลการทดลองรอบ 50 epoch (Run A/B: noaug vs aug)
เท่านั้น ไม่ใช่รอบสุดท้าย 150 epoch @ 960px ที่สไลด์และเว็บแอปใช้จริง**

ตรวจโดยแกะข้อความจริงในไฟล์ (`python zipfile` อ่าน `word/document.xml`) พบว่า:

| | ในเล่มตอนนี้ | ผลจริงล่าสุด (13-14 ส.ค.) |
|---|---|---|
| mAP@0.5 (test) | **61.46%** (`0.6146`) | **70.15%** (`0.7015`) |
| ค่าที่มา | Run B (aug, 50 epoch, 640px) | aug960 (150 epoch, 960px) |
| ผลรายคลาส needle | mAP50 0.1853-0.2515 | mAP50 0.3603 (recall 0.2435→0.3579 หลังขึ้น 960) |
| การวิเคราะห์ขนาดวัตถุ (needle 12.4px, resolution fix) | **ไม่มีเลย** (`box_size`, `imgsz`, `960` เชิงเทคนิค = 0 ครั้ง) | เป็นแกนกลางของบทที่ 5 ในสไลด์ (สไลด์ 27) |

**นี่คือ centerpiece ของโครงงานตามที่สไลด์เล่า** (needle เป็นปัญหาขนาดวัตถุ วัดได้ แก้ด้วย imgsz)
**แต่ไม่มีอยู่ในเล่มรายงานเลยแม้แต่บรรทัดเดียว** — ถ้ากรรมการเปิดเล่มเทียบกับสไลด์จะเห็นคนละเรื่อง
เสี่ยงเกณฑ์ข้อ 4 (เอกสารรายงาน 15 คะแนน) และข้อ 3.2 (สไลด์ต้องไม่ขัดกับเล่ม)

วัตถุดิบพร้อมมีอยู่แล้วที่ `05_reports/model-results-2026-08-13.md` — เหลือแค่เขียนแทรกเข้าเล่ม
(ยังไม่มีใครทำขั้นนี้)

> **มติผู้ใช้ 16 ส.ค. 2569:** ไม่แก้เล่มก่อนวันนำเสนอ — สไลด์ยึดผลล่าสุด (150 epoch @ 960px) ตามที่เป็นอยู่
> เพราะเล่มรายงานมีรอบส่งแก้ไขได้ภายหลัง (รอประกาศวันจากคณะ) ส่วนการนำเสนอครั้งนี้ต้องใช้ข้อมูลล่าสุดไปพรีเซนต์ก่อน
> รับความเสี่ยงเกณฑ์ข้อ 4/3.2 ไว้โดยตั้งใจ · คำตอบเตรียมไว้แล้วที่ `Design systems/FINAL-CONTENT.md` ภาคผนวก ง

---

## 1. รายงาน (`05_reports/แก้3-fixed.docx`)

| รายการ | สถานะ | หมายเหตุ |
|---|:---:|---|
| บทที่ 3 เขียนใหม่ตามระบบจริง (9 คลาส, ไม่ใช่ของแต่ง) | ✅ | commit `651f8e0` |
| **บทที่ 4-5 อัปเดตเป็นผลรอบ 150 epoch @ 960px** | ❌ | ดูหัวข้อสีแดงด้านบน — งานใหญ่สุดที่เหลือ |
| ตัด Google Colab / เว็บแคม / Latency ที่ไม่มีจริงออก | ✅ | ตรวจแล้ว 0 ครั้งในไฟล์ล่าสุด |
| `FPS` ที่ไม่ควรมี | ⚠️ | เหลือ 1 ครั้งใน `แก้3-fixed.docx` — ต้องเช็คว่าเป็นตัวที่ถูกต้อง (อธิบาย FPS ของวิดีโอต้นฉบับ หน้า 25) หรือของแต่งที่หลุดมา |
| `5 ประเภท` (เลขคลาสเก่า ต้องเป็น 9) | ⚠️ | เหลือ 1 ครั้ง — ยังไม่สะอาด 100% |
| ย่อหน้าขอบเขต RNN เป็น "ระยะที่สอง" | ❌ | ค้นหา `ระยะที่สอง` = 0 ครั้ง — ยังไม่ได้แทรก |
| สารบัญ / เลขรูป-ตาราง / บทคัดย่ออังกฤษ / เลขหน้าบรรณานุกรม | ⚠️ | ระบุปัญหาไว้ครบใน `edit-checklist-2026-08-12.md` หัวข้อ ค-ช แต่ต้องเปิดใน Word ตรวจเอง (แกะข้อความอย่างเดียวเช็คเลขหน้า/สารบัญไม่ได้) |
| เอกสารต้นเรื่อง (`abstract-2026-08-12.md`, `model-results-2026-08-13.md`, `ch3-replacement-p30-32.md`) พร้อมใช้ | ✅ | ตรวจตัวเลขแล้วทุกไฟล์ |

## 2. โมเดล (การเทรน)

| รายการ | สถานะ | หมายเหตุ |
|---|:---:|---|
| Baseline 9 คลาส (150 epoch, 960px) | ✅ | `models/ssid9_960px_150ep_20260813_map50-716.pt` · val mAP50 0.7160 / test 0.7015 |
| ทดลอง A/B (augmentation) — พิสูจน์ว่า aug ชนะทุกคลาส | ✅ | คุมตัวแปรสะอาด (กติกา ❶) |
| วินิจฉัย needle ว่าเป็นปัญหาขนาดวัตถุ + แก้ด้วย imgsz 960 | ✅ | `results_boxsize/` + `results_960/` — recall needle 0.2435→0.3579 (val) |
| รอบ mAP 0.80 (`train_ssid9_v2.py`) | ⏸ | **ตั้งใจเลื่อน** — ผู้ใช้สั่งไว้ว่า "อย่าพึ่งเทรน" ไม่ใช่ของค้าง |

## 3. เว็บแอป

| รายการ | สถานะ | หมายเหตุ |
|---|:---:|---|
| Phase 1 (MVP): Extract·Detect·Annotate·Export·Label Assist·Models API | ✅ | ครบทั้งหมด (สไลด์ N6) |
| Phase 2: multi-select+bulk, context menu, auto-assist, occluded/truncated, Analytics | ✅ | ครบทั้งหมด |
| Phase 3: polygon, keypoint, OCR | ✅ | ทำได้ 3 จาก 7 ที่วางแผนไว้ |
| Phase 3 ที่เหลือ (SAM auto-box, video track, multi-user, OBB) | ⏸ | ตั้งใจเลื่อน ไม่ใช่ MVP |
| ผลิตชุดข้อมูลจริงผ่านเว็บแอปเอง | ✅ | 2,726 เฟรม · 21,127 กล่อง — หลักฐาน "ใช้งานได้จริง" ที่แข็งแรงสุด |
| Deployment จริง (HTTPS, session นอก RAM, โดเมน) | ❌ | ระบุเป็นข้อจำกัดตรง ๆ ในสไลด์ N4 แล้ว (การตัดสินใจ ไม่ใช่ของลืมทำ) |
| โมเดล 9 คลาสเป็นค่าเริ่มต้นของตัวเลือกโมเดล (first-visit) | ❌ | `app.js:733` — ค้างจาก handoff ก่อนหน้า ยังไม่ได้แก้ |
| คู่มือผู้ใช้ (user manual) | ❌ | ยังไม่ได้เขียน |
| ภาพหน้าจอ (screenshots) | ⚠️ | มี 9/24 ที่วางแผนไว้ — พอสำหรับสไลด์ 38 ไม่ใช่ตัวบล็อก |

## 4. Presentation_Final (สไลด์)

| รายการ | สถานะ | หมายเหตุ |
|---|:---:|---|
| โครงสไลด์ 68 แผ่นตาม `SLIDE-BRIEF.md` | ✅ | `deck/content.py` สร้างครบ + `verify_deck.py` ผ่าน 44 จุดตรวจ |
| **เปิดไฟล์ `.pptx` ยืนยันด้วยตาว่าภาษาไทย/ฟอนต์ไม่พัง** | ❌ | PowerPoint COM ใช้ไม่ได้บนเครื่องพัฒนา — **ยังไม่มีใครเห็นสไลด์จริงเลยสักครั้ง** ต้องเปิดบนเครื่องจริงก่อนอื่นใด |
| ตัวเลขความเร็ว (N5) ใส่ในโค้ดแล้ว | ✅ | วัดจริง 14 ส.ค. — แต่ยังไม่ยืนยันว่าเรนเดอร์ถูกในไฟล์จริง (ดูแถวบน) |
| เช็คลิสต์เกณฑ์คะแนน + เนื้อหา (`PRESENTATION-CHECKLIST.md`) | ✅ | ทำเสร็จเทิร์นก่อนหน้า |
| บทพูด (speaking script → speaker notes) | ❌ | ยังไม่ได้เริ่ม |
| ชุดคำถามซ้อม (rehearsal Q&A) | ❌ | ยังไม่ได้เริ่ม — วัตถุดิบพร้อมแล้ว (กติกา ❶-❾ ใน SLIDE-BRIEF) |

---

## ลำดับที่ควรทำต่อ (เรียงตามความเสี่ยงต่อคะแนน)

1. **ตัดสินใจเรื่องบทที่ 4-5 ของรายงาน** — อย่างน้อยต้องรู้ว่าจะอัปเดตเล่มให้ตรงกับผล 960px จริง หรือ
   จะยอมรับความเสี่ยงที่กรรมการเทียบแล้วเจอเลขไม่ตรงกัน (เกณฑ์ข้อ 4 = 15 คะแนน)
2. เปิดไฟล์ `.pptx` บนเครื่องจริงยืนยันภาษาไทย/ฟอนต์ ก่อนแก้อะไรต่อ
3. บทพูด + ชุดคำถามซ้อม (งานที่ handoff ระบุไว้เป็นคิวถัดไปอยู่แล้ว)
4. งานเล็กที่เหลือ ไม่กระทบคะแนนโดยตรงแต่ควรรู้ไว้: default model picker, user manual, สารบัญ/เลขรูปในรายงาน
