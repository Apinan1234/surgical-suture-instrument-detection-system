Handoff ล่าสุดอยู่ที่:

C:\Users\USER\AppData\Local\Temp\handoff-videoframeextractor-backlog-2026-08-11.md

เซสชันนี้ทำอะไร: ผู้ใช้เลือก "ทำแผน 6 ข้อที่จดไว้" จาก 4 ทางเลือก แล้วทำจนจบทั้ง 6 ข้อในเซสชันเดียว
ตามลำดับที่แผนแนะนำ (5 -> 1 -> 2 -> 3 -> 6 -> 4) คือ freeze ฟอร์ม Export ระหว่าง build, จำ job id
ของ Extract/Export ข้าม reload (ปุ่ม ZIP ไม่หายอีก), ปุ่ม Back/Next ท้ายทุกแท็บ, ใช้โมเดลตัวเดียวทั้งแอป
พร้อม POST /api/models/load + ป้ายบอกสถานะ, POST /api/frames/review/bulk (confirm ทั้งช่วงที่ filter
กรองอยู่), และ POST /api/frames/upload (อัปโหลดโฟลเดอร์เฟรมที่ extract จากที่อื่น)

จากนั้นรัน code-review ก่อน push เจอบั๊กจริง 7 ข้อ แก้ทั้งหมดใน cc46665 — 2 ข้อพังฟีเจอร์ตัวเองเต็มๆ:
(1) อัปโหลดโฟลเดอร์ใช้ไม่ได้กับโฟลเดอร์ที่เครื่องมือนี้สร้างเอง เพราะทุกโฟลเดอร์มี extraction_log.json
    ปนอยู่ ไฟล์แรกที่ไม่ใช่รูปทำให้ 400 แล้วลบทั้ง import ทิ้ง (แก้เป็นข้ามไฟล์ที่ไม่ใช่รูป)
(2) confirm ทั้งช่วงอาจรับกล่องที่เพิ่งลบไปเป็น ground truth เพราะเฟรมที่เปิดค้างอยู่ถูกนับรวมด้วย
    ทั้งที่ยังไม่ได้ save (แก้เป็นเว้นเฟรมนั้นไว้ + บอกเหตุผลใน dialog)

สถานะตอนปิดเซสชัน (ยืนยันด้วยคำสั่งจริง ไม่ได้เชื่อจากบทสนทนา):
- main == origin/main push ครบ 9 commit (50ade55..cc46665) working tree สะอาด
- git push จาก shell ของ agent ใช้ได้แล้ว ถ้าใส่ GIT_TERMINAL_PROMPT=0 (ที่เคยค้างคือรอถาม
  credential โดยไม่มี TTY) — กฎเก่า "ห้าม push จาก agent" ยกเลิกได้ แต่ควรตั้ง timeout ไว้
- uvicorn รันที่ :8010 (PID 16432) บน .venv — ตอนเริ่มเซสชันมี 2 process และตัวที่ถือ port จริงคือ
  Python ของระบบซึ่งไม่มี ultralytics (โหลดโมเดลไม่ได้เลย) เช็ค OwningProcess ทุกครั้ง อย่าดูแค่ 200
- webapp/data/ + state.json = งานจริงของผู้ใช้ 2,726 เฟรม 21,127 กล่อง ** ห้ามลบ ** ไม่ถูกแตะทั้งเซสชัน
- เฟรมแรก suture01_frame_00000.jpg ยังติดสถานะ reviewed อยู่ (แก้: R -> Ctrl+A -> Delete -> A)
- โมเดลที่ต้องใช้: models/ssid_v6i_50ep_20260810_map50-608.pt
- งานถัดไปเป็นของผู้ใช้เหมือนเดิม: annotate ที่ระยะ keyframe 10/30/60 แล้ววัดด้วย
  measure_annotation.py --since snap_start.json

แผนงาน 6 ข้อ: C:\Users\USER\.claude\plans\needle-snoopy-salamander.md — ทำครบแล้ว มีสถานะ DONE
แผน execution: C:\Users\USER\.claude\plans\read-handoff-c-users-user-appdata-local-sprightly-dawn.md

ยังค้างอยู่: คู่มือผู้ใช้ (หลังเว็บเสร็จเท่านั้น), ข้อ 8 ปรับ UI/ขนาด canvas (ผู้ใช้เลือกข้ามไว้ก่อน),
SAM, obb ของจริง, domain/hosting + auth, figure 6-9 ที่ยังไม่เคยตรวจด้วยตา, และรอบเทรนถัดไป
