Handoff ล่าสุดอยู่ที่:

C:\Users\USER\AppData\Local\Temp\handoff-videoframeextractor-needle-2026-08-11.md

เซสชันนี้ทำอะไร: ผู้ใช้ขอให้โมเดลวาดกล่อง needle มาให้เองแทนการวาดมือทุกกล่อง — วัดจนเจอว่าเป็นสองสาเหตุ
ซ้อนกัน ไม่ใช่เรื่องเดียว: (1) checkpoint 10 epochs ได้ mAP50 แค่ 0.008 บน needle ของ val set ตัวเอง
= เทรนน้อยไป ไม่ใช่ domain gap และ (2) ต่อให้ตรวจเจอ ค่า confidence ก็ไม่เคยเกิน 0.143 จึงถูก threshold
0.25 ตัดทิ้งหมด แก้ทั้งสองทาง: เทรนใหม่ 50 epochs (needle mAP50 0.008 -> 0.114 คลาสอื่นดีขึ้นหมด) และ
เพิ่ม per-class confidence threshold ผลจริงบนฟุตเทจ: needle จาก 6 กล่อง เป็น 2,198 กล่อง

สถานะตอนปิดเซสชัน (ยืนยันด้วยคำสั่งจริง ไม่ได้เชื่อจากบทสนทนา):
- main == origin/main แล้ว push ครบ 9 commit (531b87b..50ade55) working tree สะอาด
  (ผู้ใช้รัน ! git push origin main เอง — จาก shell ของ agent ยังค้างเหมือนเดิม อย่าลอง)
- uvicorn ยังรันที่ :8010 (PID 8616) — restart ระหว่างเซสชันเพื่อโหลด server.py ใหม่
- webapp/data/ + state.json = งานจริงของผู้ใช้ 2,726 เฟรม 21,127 กล่อง ** ห้ามลบ **
- เฟรมแรก (index 0) ติดสถานะ reviewed อยู่ จึงถูก Detect รอบใหม่ข้าม ยังเป็นกล่องจากโมเดลเก่า
- โมเดลที่ต้องใช้: models/ssid_v6i_50ep_20260810_map50-608.pt (commit ไว้แล้ว มีสำเนานอกเครื่อง)
- งานถัดไปเป็นของผู้ใช้: annotate แล้ววัดด้วย measure_annotation.py --since snap_start.json

แผนงาน 6 ข้อที่จดไว้แต่ยังไม่เริ่ม (ผู้ใช้สั่งให้จดไว้เฉยๆ):
C:\Users\USER\.claude\plans\needle-snoopy-salamander.md

รายละเอียดทั้งหมดอยู่ในไฟล์ handoff ข้างบน + memory + commit message ทั้ง 9 อัน — ไม่เขียนซ้ำที่นี่
