# บันทึกการแก้ไขรายงาน — `แก้3.docx` → `แก้3-fixed.docx`

วันที่ 12 สิงหาคม 2569 · แก้ไปทั้งสิ้น **31 รายการ**

ต้นฉบับสำรองไว้ที่ `แก้3.backup-2026-08-12-2130.docx` (sha256 ตรงกับต้นฉบับ ยืนยันแล้ว)
ไฟล์ที่แก้แล้วคือ `แก้3-fixed.docx` — **ไม่ได้เขียนทับต้นฉบับ**

## เรื่องเลขหน้า

ไฟล์ `.docx` ไม่เก็บเลขหน้า — Word คำนวณตอนจัดหน้า ช่อง **หน้าในเล่ม** จึงยังว่างอยู่
เมื่อคุณเปิดไฟล์ กด Update สารบัญ แล้ว export เป็น PDF ส่งกลับมา ผมจะอ่านเลขหน้าจาก PDF นั้น
มาเติมให้ครบทุกรายการ **จะไม่เดาเลขหน้า**

## สิ่งที่ตั้งใจเก็บไว้ ไม่แก้

ย่อหน้าที่คุณเขียนไว้ถูกแล้วและผมไม่แตะ:

- คำอธิบายโมดูลสกัดเฟรมด้วย OpenCV (เดิมย่อหน้า 613)
- ย่อหน้าการทดสอบการเชื่อมต่อระหว่างโมดูล (เดิม 628) และข้อความแจ้งข้อผิดพลาด (เดิม 630)
- ผลการประเมินที่คุณแก้เองแล้ว: YOLO11n, 9 คลาสครบชื่อ, test mAP@0.5 ร้อยละ 61.46 และ
  การระบุตรง ๆ ว่าโมเดล RNN/GRU ยังไม่มีผลการประเมิน (เดิม 634-636)
- คำว่า "ทุกประการ" ที่ย่อหน้า 732 และ 971 ใช้ในความหมายถูกต้อง (ควบคุมตัวแปรให้เหมือนกัน
  ทุกประการ) ไม่ใช่การเคลมผลเกินจริง จึงไม่แก้
- ทุกการอ้างอิง `YOLOv8` ในบรรณานุกรม เพราะเป็นชื่อผลงานของผู้อื่น

---

# บทที่ 1

### ย่อหน้า 189

- **หน้าในเล่ม:** _(เติมหลัง export PDF)_
- **แก้เพราะ:** ตัด Google Colab ออก เพราะฝึกฝนบนเครื่องส่วนบุคคล (RTX 3050 Laptop) ทั้งหมด
- **เดิมเขียนว่า:**

  > ฝึกโมเดลบน Google Colab

- **แก้เป็น:**

  > ฝึกโมเดลบนเครื่องคอมพิวเตอร์ส่วนบุคคลที่ติดตั้งหน่วยประมวลผลกราฟิก

### ย่อหน้า 216

- **หน้าในเล่ม:** _(เติมหลัง export PDF)_
- **แก้เพราะ:** เปลี่ยนจากคำสัญญาว่าได้ทดลองแล้ว เป็นแผนงานระยะถัดไป เพราะบทที่ 4 ไม่มีผลของ Simple RNN / LSTM / GRU เลย
- **เดิมเขียนว่า:**

  > ทำการทดลองเปรียบเทียบประสิทธิภาพระหว่างสถาปัตยกรรมแบบต่างๆ ได้แก่ Simple RNN, LSTM (Long Short-Term Memory) และ GRU (Gated Recurrent Unit)

- **แก้เป็น:**

  > มีแผนเปรียบเทียบประสิทธิภาพระหว่างสถาปัตยกรรม Simple RNN, LSTM (Long Short-Term Memory) และ GRU (Gated Recurrent Unit) ในการดำเนินงานระยะถัดไป ซึ่งอยู่นอกขอบเขตของปริญญานิพนธ์ฉบับนี้ตามที่ระบุไว้ในหัวข้อ 1.3

### ย่อหน้า 240

- **หน้าในเล่ม:** _(เติมหลัง export PDF)_
- **แก้เพราะ:** เปลี่ยนจากคำสัญญาว่าได้ทดลองแล้ว เป็นแผนงานระยะถัดไป เพราะบทที่ 4 ไม่มีผลของ Simple RNN / LSTM / GRU เลย
- **เดิมเขียนว่า:**

  > ดำเนินการทดลองเปรียบเทียบประสิทธิภาพระหว่างสถาปัตยกรรมย่อยต่างๆ ได้แก่ Simple RNN, LSTM (Long Short-Term Memory) และ GRU (Gated Recurrent Unit)

- **แก้เป็น:**

  > มีแผนเปรียบเทียบประสิทธิภาพระหว่างสถาปัตยกรรม Simple RNN, LSTM (Long Short-Term Memory) และ GRU (Gated Recurrent Unit) ในการดำเนินงานระยะถัดไป ซึ่งอยู่นอกขอบเขตของปริญญานิพนธ์ฉบับนี้ตามที่ระบุไว้ในหัวข้อ 1.3

### ย่อหน้า 246

- **หน้าในเล่ม:** _(เติมหลัง export PDF)_
- **แก้เพราะ:** ตัด 'ความเร็วในการตอบสนอง (Latency)' ออกจากสิ่งที่ทดสอบ เพราะไม่เคยวัดค่านี้
- **เดิมเขียนว่า:**

  > ดำเนินการทดสอบระบบโดยรวม (System Testing) ทั้งในด้านความถูกต้องของการทำงาน (Accuracy) และความเร็วในการตอบสนอง (Latency) เพื่อปรับปรุงระบบให้พร้อมสำหรับการใช้งานจริง

- **แก้เป็น:**

  > ดำเนินการทดสอบระบบโดยรวม (System Testing) ในด้านความถูกต้องของการทำงานของแต่ละโมดูล และความถูกต้องของผลการตรวจจับ เพื่อปรับปรุงระบบให้พร้อมสำหรับการใช้งานจริง

### ย่อหน้า 260

- **หน้าในเล่ม:** _(เติมหลัง export PDF)_
- **แก้เพราะ:** ตัด Google Colab ออกจากรายการเครื่องมือ เพราะฝึกฝนบนเครื่องส่วนบุคคลทั้งหมด
- **เดิมเขียนว่า:**

  > - Google Colab/Jupyter Notebook ใช้สำหรับเขียนโค้ด Python และทดลองรันโมเดล

- **แก้เป็น:**

  > - Visual Studio Code และ Jupyter Notebook ใช้สำหรับเขียนโค้ด Python และทดลองรันโมเดล

# บทที่ 2

### ย่อหน้า 321

- **หน้าในเล่ม:** _(เติมหลัง export PDF)_
- **แก้เพราะ:** สลับลำดับหัวข้อ 2.1 ให้มาก่อน 2.2
- **เดิมเขียนว่า:**

  > 2.1 ความรู้พื้นฐานเกี่ยวกับอุปกรณ์ทางการแพทย์ (อยู่หลัง 2.2)

- **แก้เป็น:**

  > 2.2 ทฤษฎีที่เกี่ยวข้อง (ย้ายมาอยู่หลัง 2.1)

### ย่อหน้า 377

- **หน้าในเล่ม:** _(เติมหลัง export PDF)_
- **แก้เพราะ:** ตัดจำนวน '5 ประเภท' ออก เพราะชุดข้อมูลของโครงงานกำกับไว้ 9 คลาส การระบุ 5 ในบทที่ 2 จะขัดกับบทที่ 3 และ 4 (แก้ทีละ run เพื่อรักษารูปแบบเดิม)
- **เดิมเขียนว่า:**

  > เนื่องจากวัตถุเป้าหมายทั้ง 5 ประเภทข้างต้นมีขนาดเล็ก มีรูปทรงใกล้เคียงกัน ...

- **แก้เป็น:**

  > เนื่องจากวัตถุเป้าหมายที่ต้องตรวจจับข้างต้นมีขนาดเล็ก มีรูปทรงใกล้เคียงกัน ...

### ย่อหน้า 402

- **หน้าในเล่ม:** _(เติมหลัง export PDF)_
- **แก้เพราะ:** ตัดข้ออ้าง FPS สูง / Latency ต่ำ / Real-time ซึ่งโครงงานนี้ไม่เคยวัด
- **เดิมเขียนว่า:**

  > โครงงานนี้เลือกใช้สถาปัตยกรรมแบบ Single-Stage Detector (ตระกูล YOLO) เนื่องจากมีความเร็วในการประมวลผลเฟรมต่อวินาที (FPS) สูง มีค่าความหน่วง (Latency) ต่ำ จึงสามารถนำไปติดตั้งเพื่อแสดงผลแบบ Real-time บนระบบเว็บแอปพลิเคชันได้โดยไม่เกิดปัญหาคอขวด

- **แก้เป็น:**

  > โครงงานนี้เลือกใช้สถาปัตยกรรมแบบ Single-Stage Detector (ตระกูล YOLO) เนื่องจากประมวลผลภาพได้ในขั้นตอนเดียว จึงมีความเร็วในการอนุมานสูงกว่าสถาปัตยกรรมแบบสองขั้นตอนที่ความแม่นยำใกล้เคียงกัน และมีขนาดแบบจำลองเล็กพอที่จะฝึกฝนบนหน่วยประมวลผลกราฟิกที่มีหน่วยความจำจำกัดได้

### ย่อหน้า 411

- **หน้าในเล่ม:** _(เติมหลัง export PDF)_
- **แก้เพราะ:** ตัดข้ออ้างเรื่องรับสตรีมจากกล้องเว็บแคม ซึ่งระบบไม่มีฟีเจอร์นี้ (รับเฉพาะไฟล์ที่อัปโหลด)
- **เดิมเขียนว่า:**

  > ฝั่งลูกข่าย (Frontend/Client-side): พัฒนาขึ้นเพื่อให้ผู้ใช้งานสามารถโต้ตอบกับระบบได้อย่างสะดวก รองรับการเปิดใช้งานผ่านเว็บเบราว์เซอร์ ทำหน้าที่รับไฟล์ภาพนิ่งหรือสตรีมวิดีโอจากกล้องเว็บแคมของผู้ใช้งาน แสดงผล Bounding Box ทับซ้อนบนภาพ พร้อมทั้งสรุปจำนวนและสถิติของอุปกรณ์ที่ตรวจพบผ่าน User Interface (UI)

- **แก้เป็น:**

  > ฝั่งลูกข่าย (Frontend/Client-side): พัฒนาขึ้นเพื่อให้ผู้ใช้งานสามารถโต้ตอบกับระบบได้อย่างสะดวก รองรับการเปิดใช้งานผ่านเว็บเบราว์เซอร์ ทำหน้าที่รับไฟล์วิดีโอหรือไฟล์ภาพนิ่งที่ผู้ใช้อัปโหลด แสดงผล Bounding Box ทับซ้อนบนภาพ พร้อมทั้งสรุปจำนวนและสถิติของอุปกรณ์ที่ตรวจพบผ่าน User Interface (UI)

# บทที่ 3

### ย่อหน้า 514

- **หน้าในเล่ม:** _(เติมหลัง export PDF)_
- **แก้เพราะ:** แก้คำสะกด DataManagement เป็น Data Management
- **เดิมเขียนว่า:**

  > 3.5 การออกแบบส่วนที่ใช้ในการจัดการข้อมูล (DataManagement Design)

- **แก้เป็น:**

  > 3.5 การออกแบบส่วนที่ใช้ในการจัดการข้อมูล (Data Management Design)

### ย่อหน้า 516

- **หน้าในเล่ม:** _(เติมหลัง export PDF)_
- **แก้เพราะ:** หัวข้อใช้เลขบทที่ 4 ทั้งที่อยู่ในบทที่ 3
- **เดิมเขียนว่า:**

  > 4.3.1 โครงสร้างพจนานุกรมข้อมูล (Data Dictionary for Classes)

- **แก้เป็น:**

  > 3.5.1 โครงสร้างพจนานุกรมข้อมูล (Data Dictionary for Classes)

### ย่อหน้า 551

- **หน้าในเล่ม:** _(เติมหลัง export PDF)_
- **แก้เพราะ:** ลบหัวข้อ 3.6 ที่ชื่อซ้ำกับ 3.5 คำต่อคำ แล้วยุบเนื้อหาเข้าเป็นหัวข้อย่อยของ 3.5
- **เดิมเขียนว่า:**

  > 3.6 การออกแบบส่วนที่ใช้ในการจัดการข้อมูล (Data Management Design)

- **แก้เป็น:**

  > (ลบทิ้ง)

### ย่อหน้า 552

- **หน้าในเล่ม:** _(เติมหลัง export PDF)_
- **แก้เพราะ:** หัวข้อใช้เลขบทที่ 4 ทั้งที่อยู่ในบทที่ 3
- **เดิมเขียนว่า:**

  > 4.3.2 สถิติของชุดข้อมูล (Dataset Statistics)

- **แก้เป็น:**

  > 3.5.2 สถิติของชุดข้อมูล (Dataset Statistics)

### ย่อหน้า 607

- **หน้าในเล่ม:** _(เติมหลัง export PDF)_
- **แก้เพราะ:** แก้เกณฑ์จาก mAP@50 ≥ 85% (ขัดกับหัวข้อ 3.9.3 ที่ระบุเกณฑ์ร้อยละ 60 และสูงกว่าผลจริง 61.46)
- **เดิมเขียนว่า:**

  > ระบบกำหนดเป้าหมายประสิทธิภาพของโมเดล YOLO11 ให้มีค่า  บน Test Set [เดิมมีสมการ OMML ฝังอยู่]

- **แก้เป็น:**

  > ระบบกำหนดเกณฑ์ประสิทธิภาพของแบบจำลองไว้ที่ค่า mAP@50 ไม่ต่ำกว่าร้อยละ 60 บนชุดทดสอบ โดยใช้ผลของรอบการทดลองที่ไม่เพิ่มขยายข้อมูลเป็นเส้นฐานเปรียบเทียบ

### ย่อหน้า 608

- **หน้าในเล่ม:** _(เติมหลัง export PDF)_
- **แก้เพราะ:** ลบหัวข้อระดับบท 'การพัฒนา ติดตั้ง และทดสอบระบบ' ที่หลุดมาอยู่กลางบทที่ 3
- **เดิมเขียนว่า:**

  > การพัฒนา ติดตั้ง และทดสอบระบบ

- **แก้เป็น:**

  > (ลบทิ้ง)

### ย่อหน้า 609

- **หน้าในเล่ม:** _(เติมหลัง export PDF)_
- **แก้เพราะ:** เขียนใหม่จากคำนำระดับบท (มีคำว่า 'ในบทนี้' และ 'ระยะที่ 1') ให้เป็นคำนำระดับหัวข้อ
- **เดิมเขียนว่า:**

  > ในบทนี้เป็นการอธิบายถึงรายละเอียดกระบวนการพัฒนาซอฟต์แวร์ การจัดตั้งสภาพแวดล้อมเพื่อติดตั้งระบบต้นแบบ ตลอดจนแนวทางการทดสอบฟังก์ชันการทำงานของแบบจำลองคอมพิวเตอร์วิทัศน์ "ระบบตรวจจับอุปกรณ์การเย็บแผลด้วยปัญญาประดิษฐ์ ระยะที่ 1" เพื่อตรวจสอบความถูกต้องของระบบท่อส่งข้อมูล (Data Pipeline) ตามที่ระบุไว้ในเอกสารความต้องการของระบบ

- **แก้เป็น:**

  > หัวข้อนี้อธิบายรายละเอียดของการพัฒนาซอฟต์แวร์ สภาพแวดล้อมที่ใช้ในการพัฒนาและการทดลอง ตลอดจนแนวทางการทดสอบระบบและการประเมินแบบจำลอง

### ย่อหน้า 610

- **หน้าในเล่ม:** _(เติมหลัง export PDF)_
- **แก้เพราะ:** ไล่เลขหัวข้อจาก 5.1
- **เดิมเขียนว่า:**

  > 5.1 การพัฒนาระบบ (System Development)

- **แก้เป็น:**

  > 3.7 การพัฒนาระบบและส่วนประกอบของโปรแกรม

### ย่อหน้า 611

- **หน้าในเล่ม:** _(เติมหลัง export PDF)_
- **แก้เพราะ:** เอา Heading3 ออกจากเนื้อความ แก้ '3 โมดูล' เป็น 4 และตัดการอ้างบทที่ 2/4 ที่ไม่ตรง
- **เดิมเขียนว่า:**

  > การพัฒนาระบบตรวจจับการเย็บแผลทางการแพทย์โดยใช้ AI ได้ดำเนินการเขียนโปรแกรมและประยุกต์ใช้เทคโนโลยีตามที่ระบุไว้ในบทที่ 2 โดยจำแนกส่วนประกอบของโปรแกรม (Module) ออกเป็น 3 โมดูลหลัก เพื่อให้สอดคล้องกับกระบวนการไหลของข้อมูล (Process Design) ในบทที่ 4 ดังนี้

- **แก้เป็น:**

  > ระบบพัฒนาด้วยภาษา Python ทำงานบนเว็บเซิร์ฟเวอร์ FastAPI เพียงกระบวนการเดียว และแบ่งส่วนประกอบของโปรแกรมออกเป็น 4 โมดูลตามลำดับการไหลของข้อมูล สถานะการทำงานทั้งหมดถูกบันทึกลงไฟล์ JSON ไฟล์เดียว โดยไม่ใช้ระบบจัดการฐานข้อมูล เนื่องจากระบบนี้เป็นเครื่องมือสนับสนุนการจัดเตรียมชุดข้อมูลที่มีผู้ใช้งานคนเดียว การเก็บสถานะเป็นไฟล์เดียวทำให้ย้ายระบบไปยังเครื่องอื่นได้ด้วยการคัดลอกโฟลเดอร์ และสำรองข้อมูลได้ด้วยการคัดลอกไฟล์เพียงไฟล์เดียว

### ย่อหน้า 612

- **หน้าในเล่ม:** _(เติมหลัง export PDF)_
- **แก้เพราะ:** ไล่เลขหัวข้อจาก 5.1.1
- **เดิมเขียนว่า:**

  > 5.1.1 โมดูลจัดการวิดีโอและเตรียมข้อมูลภาพ (Video Pre-processing Module)

- **แก้เป็น:**

  > 3.7.1 โมดูลสกัดเฟรมภาพจากวิดีโอ (Frame Extraction Module)

### ย่อหน้า 614

- **หน้าในเล่ม:** _(เติมหลัง export PDF)_
- **แก้เพราะ:** ตัดการอ้าง 'ตาราง Videos ของฐานข้อมูล' เพราะระบบไม่ได้ใช้ฐานข้อมูล
- **เดิมเขียนว่า:**

  > ความเกี่ยวข้องกันของไฟล์และข้อมูล โมดูลนี้จะบันทึกข้อมูลรายละเอียดของวิดีโอลงในตาราง Videos ของฐานข้อมูล และสร้างโฟลเดอร์ชั่วคราว (Temporary Directory) เพื่อจัดเก็บภาพเฟรมที่สุ่มได้

- **แก้เป็น:**

  > ความเกี่ยวข้องกันของไฟล์และข้อมูล โมดูลนี้จะบันทึกรายละเอียดของวิดีโอและรายการเฟรมที่สกัดได้ ลงในไฟล์สถานะของระบบ และจัดเก็บไฟล์ภาพเฟรมไว้ในโฟลเดอร์ของงานนั้น

### ย่อหน้า 615

- **หน้าในเล่ม:** _(เติมหลัง export PDF)_
- **แก้เพราะ:** แก้ YOLOv8 เป็น YOLO11n และตัดการอ้างตาราง Detection_Results ของฐานข้อมูล
- **เดิมเขียนว่า:**

  > 5.1.2 โมดูลตรวจจับวัตถุและเครื่องมือแพทย์ (Object Detection Core Module) รายละเอียดการพัฒนา พัฒนาขึ้นโดยใช้เทคโนโลยี Deep Learning ผ่านเฟรมเวิร์ก PyTorch และใช้แบบจำลอง YOLOv8 ที่ผ่านการฝึกฝน (Trained Model) ด้วยชุดข้อมูลภาพเครื่องมือแพทย์ที่คณะผู้จัดทำได้ทำ Labeling ไว้แล้ว โค้ดในส่วนนี้จะทำหน้าที่รับเฟรมภาพนิ่งเข้ามาประมวลผลทีละภาพ (Inference) เพื่อคำนวณหาตำแหน่งพิกัดของวัตถุ ความเกี่ยวข้องกันของไฟล์และข้อมูล ผลลัพธ์ที่ได้จากโมดูลนี้ประกอบด้วย รหัสประเภทวัตถุ (Class ID) และพิกัดกล่องข้อความ (Bounding Box Coordinates) ซึ่งจะถูกแปลงให้อยู่ในรูปแบบโครงสร้างข้อมูล JSON และบันทึกลงสู่ตาราง Detection_Results

- **แก้เป็น:**

  > 3.7.2 โมดูลตรวจจับวัตถุ (Object Detection Module) + พัฒนาด้วยเฟรมเวิร์ก PyTorch ร่วมกับไลบรารี Ultralytics โดยใช้แบบจำลอง YOLO11n ที...

### ย่อหน้า 616

- **หน้าในเล่ม:** _(เติมหลัง export PDF)_
- **แก้เพราะ:** ลบโมดูล RNN/GRU + State Machine + ตาราง Assessment_Reports + ส่งออกรายงาน PDF (ไม่มีอยู่จริง) แทนด้วยโมดูลกำกับข้อมูล โมดูลส่งออก และการควบคุมการเข้าถึง
- **เดิมเขียนว่า:**

  > 5.1.3 โมดูลวิเคราะห์และตรวจทานลำดับขั้นตอนเชิงเวลา (Temporal & Sequence Verification Module) รายละเอียดการพัฒนา เป็นโมดูลที่ใช้ภาษา Python ร่วมกับ PyTorch ในการสร้างสถาปัตยกรรมโครงข่ายประสาทเทียมแบบหมุนเวียน RNN/GRU เพื่อทำหน้าที่รับข้อมูลพิกัดและประเภทวัตถุที่มีการเรียงลำดับตามแกนเวลาเข้ามาประมวลผลร่วมกัน โมดูลนี้จะจำแนกสถานะกิจกรรมปัจจุบัน (เช่น กำลังปักเข็ม กำลังดึงไหม) จากนั้นจะส่งข้อมูลผลลัพธ์เข้าสู่อัลกอริทึม State Machine เพื่อเปรียบเทียบกับขั้นตอนมาตรฐานทางการแพทย์และคำนวณคะแนนความถูกต้อง ความเกี่ยวข้องกันของไฟล์และข้อมูล โมดูลนี้จะสรุปคะแนน สถิติเวลา และข้อผิดพลาดทั้งหมด นำไปบันทึกลงในตาราง Assessment_Reports เพื่อสร้างหน้าจอแสดงผลลัพธ์ (Dashboard) และฟังก์ชันส่งออกรายงาน PDF

- **แก้เป็น:**

  > 3.7.3 โมดูลกำกับและตรวจทานข้อมูล / 3.7.4 โมดูลส่งออกชุดข้อมูล / 3.7.5 การควบคุมการเข้าถึงระบบ

### ย่อหน้า 617-624

- **หน้าในเล่ม:** _(เติมหลัง export PDF)_
- **แก้เพราะ:** แทนหัวข้อ 5.2 การติดตั้งระบบ (กล้องบนขาตั้ง, RTX 3060 ผ่าน USB 3.0, Ubuntu 22.04, CUDA 11.8, Docker) ด้วยสภาพแวดล้อมจริง
- **เดิมเขียนว่า:**

  > ติดตั้งกล้อง 1080p บนขาตั้ง เชื่อมเซิร์ฟเวอร์ RTX 3060 ผ่าน USB 3.0 / Ubuntu 22.04 + CUDA 11.8 + Docker

- **แก้เป็น:**

  > 3.8 สภาพแวดล้อมการพัฒนาและการทดลอง + การพัฒนาและการฝึกฝนแบบจำลองทั้งหมดดำเนินการบนเครื่องคอมพิวเต / ด้านฮาร์ดแวร์ ใช้เครื่องคอมพิวเตอร์โน้ตบุ๊กที่ติดตั้งหน่วยปร / ด้านซอฟต์แวร์ ใช้ภาษา Python รุ่น 3.10.0 ร่วมกับเฟรมเวิร์กกา / ข้อจำกัดด้านหน่วยความจำของหน่วยประมวลผลกราฟิกที่มีเพียง 4 กิ

### ย่อหน้า 625

- **หน้าในเล่ม:** _(เติมหลัง export PDF)_
- **แก้เพราะ:** ไล่เลขหัวข้อจาก 5.3
- **เดิมเขียนว่า:**

  > 5.3 การทดสอบระบบ (System Testing)

- **แก้เป็น:**

  > 3.9 การทดสอบระบบและการประเมินแบบจำลอง

### ย่อหน้า 626

- **หน้าในเล่ม:** _(เติมหลัง export PDF)_
- **แก้เพราะ:** ตัดการอ้างถึงเอกสารทดสอบระบบที่ไม่มีอยู่จริง
- **เดิมเขียนว่า:**

  > กระบวนการทดสอบระบบถูกแบ่งออกเป็น 2 ส่วนหลัก เพื่อตรวจสอบทั้งความถูกต้องในการทำงานทางเทคนิคของโปรแกรม (Functional Testing) และความแม่นยำของแบบจำลองปัญญาประดิษฐ์ (AI Model Evaluation) โดยอ้างอิงเกณฑ์ตามที่กำหนดไว้ในเอกสารทดสอบระบบ (TestDocument) ดังนี้

- **แก้เป็น:**

  > การทดสอบระบบแบ่งออกเป็นสองส่วน คือการทดสอบความถูกต้องเชิงหน้าที่ของโปรแกรม (Functional Testing) และการประเมินประสิทธิภาพของแบบจำลอง (Model Evaluation) โดยใช้ตัวชี้วัดและโปรโตคอลการวัดผลตามที่กำหนดไว้ในหัวข้อ 3.9.3

### ย่อหน้า 627

- **หน้าในเล่ม:** _(เติมหลัง export PDF)_
- **แก้เพราะ:** ไล่เลขหัวข้อจาก 5.3.x เป็น 3.9.x
- **เดิมเขียนว่า:**

  > 5.3.1 การทดสอบการทำงานของฟังก์ชันซอฟต์แวร์ (Functional and Integration Testing)

- **แก้เป็น:**

  > 3.9.1 การทดสอบการทำงานของฟังก์ชันซอฟต์แวร์ (Functional and Integration Testing)

### ย่อหน้า 629

- **หน้าในเล่ม:** _(เติมหลัง export PDF)_
- **แก้เพราะ:** ไล่เลขหัวข้อจาก 5.3.x เป็น 3.9.x
- **เดิมเขียนว่า:**

  > 5.3.2 การแสดงข้อความแจ้งเตือนและข้อผิดพลาด (Error Messages)

- **แก้เป็น:**

  > 3.9.2 การแสดงข้อความแจ้งเตือนและข้อผิดพลาด (Error Messages)

### ย่อหน้า 631

- **หน้าในเล่ม:** _(เติมหลัง export PDF)_
- **แก้เพราะ:** ไล่เลขหัวข้อจาก 5.3.x เป็น 3.9.x
- **เดิมเขียนว่า:**

  > 5.3.3 การประเมินประสิทธิภาพของแบบจำลอง AI (AI Model Evaluation)

- **แก้เป็น:**

  > 3.9.3 การประเมินประสิทธิภาพของแบบจำลอง (Model Evaluation)

### ย่อหน้า 632

- **หน้าในเล่ม:** _(เติมหลัง export PDF)_
- **แก้เพราะ:** แก้ 'Test Set จำนวน 15 วิดีโอ'
- **เดิมเขียนว่า:**

  > จากการนำคลิปวิดีโอชุดใหม่ที่ไม่เคยใช้ในการฝึกฝนโมเดลมาก่อน (Test Set) จำนวน 15 วิดีโอ มาทำการทดสอบระบบ สามารถสรุปผลความแม่นยำได้ดังนีh

- **แก้เป็น:**

  > จากการประเมินแบบจำลองบนชุดทดสอบ (Test Set) จำนวน 215 ภาพ ซึ่งไม่เคยถูกใช้ในการฝึกฝนหรือการเลือกแบบจำลองมาก่อน สามารถสรุปผลความแม่นยำได้ดังนี้

# บทที่ 4

### ย่อหน้า 932

- **หน้าในเล่ม:** _(เติมหลัง export PDF)_
- **แก้เพราะ:** หัวข้อผลเว็บแอปไม่มีเลขกำกับ ใส่เลข 4.3 ให้ (เดิมสารบัญวางไว้ใต้ 4.3 RNN)
- **เดิมเขียนว่า:**

  > ผลการพัฒนาเว็บแอปพลิเคชัน

- **แก้เป็น:**

  > 4.3 ผลการพัฒนาเว็บแอปพลิเคชัน

# บทที่ 5

### ย่อหน้า 977

- **หน้าในเล่ม:** _(เติมหลัง export PDF)_
- **แก้เพราะ:** เขียนข้อเสนอแนะเรื่อง RNN ใหม่ ให้เป็นงานระยะถัดไปตรงกับขอบเขตในหัวข้อ 1.3
- **เดิมเขียนว่า:**

  > ควรดำเนินการเก็บผลการทดลองของแบบจำลอง RNN และการพัฒนาเว็บแอปพลิเคชันให้ครบถ้วน เพื่อประเมินประสิทธิภาพของระบบโดยรวมทั้งหมด ตั้งแต่การตรวจจับอุปกรณ์ การจำแนกลำดับขั้นตอน ไปจนถึงการนำเสนอผลผ่านเว็บแอปพล

- **แก้เป็น:**

  > ควรพัฒนาแบบจำลองวิเคราะห์ลำดับเหตุการณ์เชิงเวลาในระยะถัดไป เพื่อจำแนกลำดับขั้นตอนการเย็บแผล ซึ่งต้องอาศัยการกำกับข้อมูลระดับขั้นตอนเพิ่มเติมจากชุดข้อมูลที่มีอยู่ โดยอยู่นอกขอบเขตของปริญญานิพนธ์ฉบับนี้ตามที่ระบุไว้ในหัวข้อ 1.3

---

## การตรวจที่ผ่านแล้ว

ตรวจจาก**ข้อความที่ประกอบร่างจากทุก run แล้ว** ไม่ใช่ค้นใน XML ดิบ — เพราะ Word ซอยข้อความ
เป็นหลายก้อน การค้น XML ดิบจะรายงานว่า "ไม่พบ" ทั้งที่ข้อความยังอยู่ (เจอกับตัวเองที่ย่อหน้า 377)

| ตรวจอะไร | ผล |
|---|---|
| ไม่เหลือคำว่า `88.5` | ผ่าน |
| ไม่เหลือคำว่า `85.2` | ผ่าน |
| ไม่เหลือคำว่า `RTX 3060` | ผ่าน |
| ไม่เหลือคำว่า `Docker` | ผ่าน |
| ไม่เหลือคำว่า `Ubuntu` | ผ่าน |
| ไม่เหลือคำว่า `Google Colab` | ผ่าน |
| ไม่เหลือคำว่า `เว็บแคม` | ผ่าน |
| ไม่เหลือคำว่า `Assessment_Reports` | ผ่าน |
| ไม่เหลือคำว่า `Detection_Results` | ผ่าน |
| ไม่เหลือคำว่า `15 วิดีโอ` | ผ่าน |
| ไม่เหลือคำว่า `ทั้ง 5 ประเภท` | ผ่าน |
| `document.xml` แปลงเป็น XML ได้ไม่ error | ผ่าน |
| ไฟล์ zip ครบ 50 part เท่าเดิม ไม่หายไม่เพิ่ม | ผ่าน |
| ลำดับหัวข้อ 2.1 มาก่อน 2.2 | ผ่าน |
| บทที่ 3 ไม่มีหัวข้อเลข 5.x เหลืออยู่ | ผ่าน |

## ที่ยังไม่ได้ทำ (รอคุณตรวจเนื้อหาก่อน)

1. ใส่ diagram 11 รูปที่ต้องมี + 5 รูปที่ใส่ก็ได้ (ข้าม B2 deployment เพราะขัดกับบทที่ 3 ที่แก้แล้ว)
2. จัด Heading style ให้หัวข้อทั้ง 59 อัน — เงื่อนไขจำเป็นของสารบัญอัตโนมัติ
3. ลบสารบัญพิมพ์มือ แล้วใส่ TOC field + สารบัญรูป + สารบัญตาราง
4. ลบ `w:start="1"` จาก section ท้ายเล่ม ให้เลขหน้าบรรณานุกรมนับต่อ
5. เขียนหัวข้อ 2.2.6 Object Detection ที่หายไปจากเนื้อเล่ม
