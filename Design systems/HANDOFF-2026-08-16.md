---
name: handoff-videoframeextractor-2026-08-16-new-account
description: Continuation handoff for a Claude Code session started under a DIFFERENT Anthropic account, same machine, same project folder. Content-writing for the Final Presentation deck is done; the next session's job is review-with-user then mechanical conversion to a real .pptx.
---

# Handoff — VideoFrameExtractor, 2026-08-16 → next session (new login, same folder)

**Read this file first, then act on it directly** — the user's opening message will likely just be
"read the handoff and continue" or similar. This document IS your task brief; you don't need to ask
the user to re-explain anything below before starting step 1 in "What to do next."

## Why this handoff exists

The previous session (different Anthropic account, same machine, same project directory
`C:\Users\USER\OneDrive\Documents\BEAM\VideoFrameExtractor`) ran low on credits mid-task. All content
work is finished; what's left is mechanical (convert → build → verify a PowerPoint deck). Nothing about
switching accounts changes anything on disk — project files, `.claude/` memory, and git state are all
local to this machine + folder, not tied to which email is logged in.

## Where things stand — read these two files in full before touching anything

| File | What it is | Status |
|---|---|---|
| `Design systems/design-system-final-presentation.md` | Design rules: fonts, colors, archetypes, content rules, and a full digest of 3 reference decks (Stanford CS231n + **two real senior capstone decks from the same major/advisor**, found by the user) | **Done.** ~650 lines. |
| `Design systems/FINAL-CONTENT.md` | The actual slide content: 81 main slides + 17 backup slides. Every slide has on-screen text, image path, full speaking script (in the actual presenter's voice/pronouns), source citation, anticipated committee question | **Done, numbers audited against source files, not yet confirmed by the user as final.** |
| `Design systems/HANDOFF-2026-08-16.md` | A durable (repo-committed-path, survives `%TEMP%` clearing) copy of the *previous* handoff from this same detour | Superseded by this file, but harmless to leave — same facts, this one is more complete |

**Do not re-read the full content of those two files into this handoff** — they're large and already
final; go read them directly in the repo instead of trusting a summary.

### Key facts already established (don't re-derive these)

- **Speakers assigned, real names + student IDs, already written into every relevant slide:**
  ตรีฤทัย แคยิหวา (66120361, report) → อภินันท์ อายุยงค์ (66112772, model+webapp, opens section 2)
  → ฟ้าใส ขวัญปาน (66126467, dataset annotation, section 3 + live demo) → อภินันท์ closes (sections 4-8).
- **Training that this content depended on is finished**: `baseline9-reannot`, 150/150 epochs, on the
  re-annotated dataset (`D:\ml\runs\ssid9_reannot_baseline9-reannot_150ep`). Re-validated results are
  already written into slide 5-18: val mAP50 **0.7225**, test mAP50 **0.7212**, needle recall
  0.3579→0.4229. **Verify this is still true** (`results_reannot/benchmark_comparison.md`) — it's been
  a while since last checked and this fact could theoretically go stale if anyone reruns training.
- **The two peer example decks** are at `Design systems/UFPs-Final-Presentation-1.pdf` and
  `Design systems/สำเนาของ-Final-Project.pdf` — both from Walailak University, "Computer Engineering
  and Artificial Intelligence" (same major name as this team's), same advisor. A custom PDF text
  extractor was written to read them (this machine has zero PDF tooling — see project memory
  `reference_reading_pdfs_here`). The extractor script, if ever needed again, is described in
  `design-system-final-presentation.md` §1.7 but was written to a session-scratchpad path that no
  longer exists — rewrite it if a third PDF ever needs digesting (it's ~150 lines, straightforward:
  parse PDF objects with a regex, handle `/ObjStm` if present, decode `ToUnicode` CMaps, and for
  Canva-exported PDFs specifically, hex strings are 2-byte CIDs — see §1.7 for the exact gotcha).

## Non-negotiable rules — violating any of these is worse than being late

All numbered rules ❶–⓭ in `design-system-final-presentation.md` §7 (which itself points to the fuller
originals in `Design systems/SLIDE-BRIEF.md` §0). **The one most likely to get broken by someone moving
fast:** never phrase the re-annotated-dataset run (0.7225) as "beating" the original run (0.7160) —
they use different validation splits, so it's not a valid comparison. The correct phrasing is already
written into slide 5-18 — copy it, don't rephrase it from memory.

Also: every number in every slide must trace to a real file (already done in `FINAL-CONTENT.md` — when
transcribing into `content.py`, don't "clean up" or round numbers differently than the source file says).

## What to do next (in order)

1. **Confirm with the user** that `Design systems/FINAL-CONTENT.md` is approved to build as-is, or
   whether they want changes first. (It has not been explicitly approved yet — the previous session
   ran out of budget partway through review, not partway through writing.)
2. **Translate `FINAL-CONTENT.md` into `Design systems/deck/content.py`** using the archetype functions
   already defined in `Design systems/deck/layouts.py` (`cover()`, `section()`, `bullets()`, `diagram()`,
   `tall_diagram()`, `metric_row()`, `two_up()`, `big_number()`, `table()`, `screenshot_grid()`). This is
   transcription, not new content design — every string, bullet, and source citation in `content.py`
   should come directly from `FINAL-CONTENT.md`. The current `content.py` (71 slides, pre-dates this
   round of additions) is the pattern to follow for code style/structure.
3. **Build and verify**: from `Design systems/deck/`, run
   `python build_deck.py && python verify_deck.py` — both must pass clean.
4. **Known unresolved issue, not blocking**: PowerPoint COM doesn't work on this dev machine
   (`0x80048240`) — nobody has visually opened the actual `.pptx` yet. Flag this to the user; someone
   needs to open the file on a real machine before the exam to check Thai text/fonts render correctly.
5. **Optional, low priority, not started**: a "model detects correctly" example image for slide 0-4
   (currently has a placeholder note in its own text). Needs running inference with
   `D:\ml\runs\ssid9_reannot_baseline9-reannot_150ep\train\weights\best.pt` on one held-out test frame
   and saving the annotated result into `Design systems/assets/`. Only do this if the user asks.
6. Do **not** touch anything under `D:\ml\data\ssid9_reannot*`, `D:\ml\runs\ssid9_reannot_*`, or run
   `powercfg` sleep-timeout changes without checking training is truly finished first (it was, as of
   this handoff — but re-verify, don't assume).

## Suggested skills for this session

- No packaged skill matches "transcribe markdown into python-pptx calls, then run existing build
  scripts" — this is direct file editing + running `build_deck.py`/`verify_deck.py`, which are already
  project-specific scripts, not something a general skill wraps.
- **`code-review`** (low priority, optional) — worth a quick pass over the new `content.py` diff after
  step 2, specifically checking that no slide silently dropped a source citation or a warning callout
  during transcription (easy mistake when moving 80+ slides of prose into function calls).
- **`run`** — only relevant if/when the user wants to launch the actual webapp for a live demo
  rehearsal or to grab a fresh screenshot; not needed for the deck-building task itself.

## Sensitive-info check

Reviewed for this handoff: no API keys, tokens, or passwords appear anywhere above. The webapp's
`APP_PASSWORD` is referenced only by file path (`01_frame-extractor-tool/webapp/.env`), never by value.
Student names + ID numbers are the user's own team's presentation content (not third-party PII) and are
required for the task, so they're included as-is.

## Working notes (same as every session on this project)

User works in Thai, expects Thai replies, delegates strategy, dislikes being asked many questions —
pick a sensible default, do it, say what was done. Broader project memory (webapp history, model
history, every prior decision) loads automatically via the standard project-memory recall; this handoff
only covers what's new/ephemeral from the current detour and isn't in memory yet.
