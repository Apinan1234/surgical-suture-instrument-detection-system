# ASSET-INDEX — ไฟล์ทุกตัวในแพ็กนี้ อธิบายว่ามันแสดงอะไร

คู่กับ `SLIDE-BRIEF.md` · ทุกไฟล์เป็นสำเนา ตรวจแล้วว่าตรงกับต้นฉบับทุกไบต์ (`diff -rq`)
ต้นฉบับยังอยู่ที่เดิม แพ็กนี้ไม่ได้ย้ายอะไรออกจากโปรเจกต์

**สรุปจำนวน:** diagram 21 (PNG 21 + SVG 21) · ภาพหน้าจอ 17 · ผลโมเดล 228 ไฟล์ · เอกสารต้นทาง 9 + โน้ตบุ๊ก 3


## เพิ่ม 14 ส.ค. 2569 — `assets/results-perf/` (ผลวัดความเร็ว)

| ไฟล์ | แสดงอะไร | สไลด์ |
|---|---|---|
| `inference_benchmark.md` | ความเร็วการอนุมานที่วัดจริง CPU กับ GPU พร้อมข้อจำกัดว่าอะไรยังไม่ได้วัด | N5 (สองแผ่น) |
| `bench_cpu.json` / `bench_gpu.json` | ผลดิบของการวัดแต่ละรอบ | - |

ต้นฉบับอยู่ที่ `01_frame-extractor-tool/results_960/inference_benchmark.md` · สคริปต์ที่ใช้วัดคือ
`01_frame-extractor-tool/benchmark_inference.py` (อ่านอย่างเดียว ไม่แตะ `state.json`)

**ตัวเลขนี้ปลดล็อกกติกา ❹** ซึ่งเดิมห้ามพูดถึงความเร็วเพราะไม่เคยวัด

---

## 1. `assets/diagrams-svg/` และ `assets/diagrams-png/` — 20 รูป

ไฟล์ชื่อเดียวกันทั้งสองโฟลเดอร์ ต่างแค่นามสกุล **ใช้ SVG ก่อนเสมอ** PNG เป็น 2x ไว้เผื่อ

### ชุด ก — ฝั่งแบบจำลอง (A1-A8)

| ไฟล์ | ชื่อในรายงาน | ข้อความหลักของรูป | สไลด์ |
|---|---|---|---|
| `A1-ml-pipeline` | ก1 · กระบวนการ ML | กระบวนการเป็น **วงจร ไม่ใช่เส้นตรง** — โมเดลที่เทรนเสร็จถูกส่งกลับไปช่วยกำกับข้อมูลรอบถัดไป | 6 |
| `A2-label-defect` | ก2 · ข้อบกพร่องป้ายกำกับ | ข้อบกพร่องนี้ **เงียบ** — ไม่มี error ไม่มี warning ตัวเลขดูปกติ แต่กล่องผิด | 13, B10 |
| `A3-class-taxonomy` | ก3 · โครงสร้างคลาส | ระยะที่ 2 เพิ่ม 4 คลาสอย่างมีเหตุผล — แยก "ปลายเครื่องมือ" ออกจากตัวเครื่องมือ | 10 |
| `A4-yolo11-architecture` | ก4 · สถาปัตยกรรม YOLO11 | ทำนายที่ **สามความละเอียดพร้อมกัน** ชั้น P3 (80×80) คือชั้นที่รับผิดชอบวัตถุเล็ก | 15, 27 |
| `A5-ab-experiment` | ก5 · การออกแบบ A/B | ความน่าเชื่อถืออยู่ที่ **จำนวนตัวแปรที่ต่างกันต้องเท่ากับหนึ่ง** | 17 |
| `A6-flowchart-label-cleaning` | ก6 · ผังงานทำความสะอาด | ทางออกทุกทางถูกนับและรายงาน ไม่มีแถวใดหายไปเงียบ ๆ | 14 |
| `A7-flowchart-training` | ก7 · ผังงานการฝึก | `best.pt` กับ `last.pt` ไม่ใช่ไฟล์เดียวกัน | 16 |
| `A8-flowchart-inference` | ก8 · ผังงานการอนุมาน | การกรองเกิด **สองชั้น** — ไลบรารีกรองที่เกณฑ์ต่ำสุด แล้วโค้ดเรากรองซ้ำรายคลาส | 18 |

### ชุด ข — ฝั่งเว็บแอป (B1-B12, 13 รูปเพราะ B10 มีสองแบบ)

| ไฟล์ | ชื่อในรายงาน | ข้อความหลักของรูป | สไลด์ |
|---|---|---|---|
| `B1-system-architecture` | ข1 · สถาปัตยกรรม | เดสก์ท็อปเดิมกับเว็บแอปใหม่เป็น "ผู้ใช้" คนละตัวของ **โมดูลแกนกลางชุดเดียวกัน** | 7 |
| `B2-deployment` | ข2 · Deployment | ทั้งระบบติดตั้งบนเครื่องเดียว เบราว์เซอร์กับเซิร์ฟเวอร์อยู่เครื่องเดียวกัน | B12 |
| `B3-context-diagram` | ข3 · Context | มีตัวแทนภายนอกแค่ **2 ตัว** — ผู้ใช้ และ Roboflow Cloud (Tesseract รันในเครื่อง ไม่นับ) | 8 |
| `B4-dfd-level1` | ข4 · DFD ระดับ 1 | **`state.json` (D3) คือศูนย์กลาง** — โพรเซสตรวจจับ กำกับข้อมูล ส่งออก ใช้ที่เดียวกัน | 30 |
| `B5-use-case` | ข5 · Use Case | เป้าหมายหลัก 8 อย่าง "กำกับข้อมูลภาพ" มีรูปแบบย่อย 3 ชนิด | 29 |
| `B6-sequence-extract` | ข6 · ลำดับสกัดเฟรม | ข้อความที่ 4 คือหัวใจ — เซิร์ฟเวอร์ตอบกลับ **ก่อน** งานเสร็จ ⚠️ ยังไม่เคยตรวจด้วยตา | 32 |
| `B7-sequence-assist` | ข7 · ลำดับ Label Assist | กรอบ `alt` แรกคือมาตรการป้องกันเครดิต — ถามยืนยันก่อนยิงออกคลาวด์ ⚠️ ยังไม่เคยตรวจด้วยตา | 33 |
| `B8-flowchart-extract` | ข8 · ผังงานสกัดเฟรม | เฟรมไม่ผ่านเกณฑ์ → ไม่ทิ้งทั้งช่วง แต่ย้อนกลับไปจุดตัดสินใจ ⚠️ ยังไม่เคยตรวจด้วยตา | 31 |
| `B9-activity-annotate` | ข9 · วงจรกำกับข้อมูล | กิจกรรมกรอบแดงคือจุดสำคัญที่สุดของวงจร ⚠️ ยังไม่เคยตรวจด้วยตา | 34 |
| `B10-state-machine` | ข10 · สถานะของงาน | ทุกงานใช้ชุดสถานะเดียวกัน **ไม่มีสถานะรอคิว** เพราะสร้างเธรดแล้วเริ่มทันที | 35 |
| `B10-state-machine-2` | ข10 · สถานะของเฟรม | เส้นย้อนจาก "กำกับและรีวิวแล้ว" กลับไป "รอการยืนยัน" คือจุดที่ผู้ใช้แก้งานตัวเองได้ | 36 |
| `B11-data-model` | ข11 · โครงสร้างข้อมูล | **FRAME คือศูนย์กลาง** — ทั้งผลผลิตของการสกัด เป้าหมายของการตรวจจับ และวัตถุของการกำกับ | 37 |
| `B12-system-framework` | ข12 · System Framework | **Frontend / Server / Database** — บริการจริงมีแค่ Server เดียว (FastAPI+Uvicorn, 38 endpoint) Frontend เป็นไฟล์ static ที่เสิร์ฟจากโปรเซสเดียวกัน และ "ไม่มีฐานข้อมูล" คือการตัดสินใจ ไม่ใช่ข้อจำกัด | ส่วน 6 (สไลด์ใหม่ ระหว่าง "ฟังก์ชันของระบบทั้งหมด" กับ "Infrastructure") |

> ⚠️ = ตรวจเชิงโครงสร้างผ่าน แต่การ capture หน้าเพจค้างทุกครั้ง จึงยังไม่เคยเห็นด้วยตาว่า
> label ไม่ล้นกรอบ **เปิดดูก่อนเอาเข้าสไลด์**

---

## 2. `assets/results-960/` — แบบจำลองที่ดีที่สุด (23 ไฟล์ × 2)

`val/` = ชุดตรวจสอบ (400 ภาพ) · `test/` = ชุดทดสอบที่โมเดลไม่เคยเห็น (215 ภาพ)
ชื่อไฟล์เหมือนกันทุกตัวระหว่างสองโฟลเดอร์ จึงต้องแยกโฟลเดอร์ ห้ามรวม

ต้นฉบับอยู่ที่ `01_frame-extractor-tool/results_960/` (val) และ `results_960_test/` (test)
ตรวจแล้วด้วย `diff -rq` ตรงกันทุกไบต์ (15 ส.ค. 2569)

**โมเดล:** `ssid9_960px_150ep_20260813_map50-716.pt` · YOLO11n · 150 epochs · imgsz 960 · batch 8 ·
seed 42 · RTX 3050 Laptop 4 GB · เวลาเทรน 97.9 นาที · checkpoint ที่เก็บคือ **epoch 86**

| ไฟล์ | แสดงอะไร | สไลด์ |
|---|---|---|
| `aug960_overall_metrics.md` / `.csv` | **ตัวเลขหัวข่าว** val: mAP50 0.7160 / mAP50-95 0.4532 / P 0.7335 / R 0.7173 · test: 0.7015 / 0.4348 / 0.7331 / 0.6926 | 23 |
| `aug960_per_class_metrics.md` / `.csv` | ตาราง AP รายคลาสทั้ง 9 คลาส พร้อมจำนวน instance | 24 |
| `aug960_per_class_map50.png` | กราฟแท่ง mAP50 รายคลาส — เห็น needle ต่ำโดดทันที | 24 |
| `aug960_confusion_matrix_normalized.png` | **needle ที่พลาดถูกอ่านเป็น background ไม่ใช่สับสนกับคลาสอื่น** สัดส่วน 0.45 → 0.37 เมื่อขึ้น 960 | 25 |
| `aug960_confusion_matrix_raw.png` | เมทริกซ์แบบนับจำนวนดิบ ใช้คู่กับตัวบน | 25 |
| `aug960_pr_curve_per_class.png` | เส้น Precision-Recall รายคลาส | 26 |
| `aug960_f1_confidence_curve.png` | **กราฟที่ใช้เลือกจุดทำงานจริง** (เว็บแอปตั้ง conf = 0.25) | 26 |
| `aug960_precision_confidence_curve.png` · `aug960_recall_confidence_curve.png` | P และ R แยกตามค่า confidence | B ตอบคำถาม |
| `aug960_learning_curve_map.png` | mAP ตามรอบการฝึก | 20 |
| `aug960_precision_recall_per_epoch.png` | P/R ตามรอบการฝึก | B ตอบคำถาม |
| `aug960_loss_curve.png` · `aug960_loss_breakdown.png` | loss รวม และแยกสามตัว — **YOLO11 มี box / cls / dfl ไม่มี objectness** (นั่นคือ v5/v7) | B6 |
| `aug960_box_loss.png` · `aug960_cls_loss.png` · `aug960_dfl_loss.png` | loss แต่ละตัวแยกรูป | B6 |
| `aug960_lr_schedule.png` | ตารางอัตราการเรียนรู้ | B7 |
| `aug960_error_analysis_grid.png` | **ตัวอย่างภาพที่โมเดลทำผิดจริง** — วัตถุดิบตอบคำถามชั้นดี | B8 |
| `class_distribution.png` / `.md` / `.csv` | จำนวนกล่องต่อคลาสต่อ split (แกนตั้ง log) finger 9,884 → Stitch Scissors 29 | 12 |
| `.metrics_aug960_{val,test}.json` | ตัวเลขดิบที่รูปทั้งหมดสร้างมาจาก — ไฟล์ซ่อน ใช้ตอนต้องอ้างที่มา | — |

---

## 3. `assets/results-comparison/` — การทดลอง A/B สี่รอบ (91 ไฟล์ × 2)

สี่รอบ: `noaug` (ปิด augmentation) · `aug` (เปิด, 50 epochs) · `aug150` (เปิด, 150 epochs, 640) ·
`aug960` (เปิด, 150 epochs, 960) — ทุกไฟล์ผลรายรอบใช้ชื่อเดียวกับหัวข้อ 2 แต่ขึ้นต้นด้วยชื่อรอบ

ต้นฉบับอยู่ที่ `01_frame-extractor-tool/results_all/` (val) และ `results_all_test/` (test) —
ตรวจแล้วด้วย `diff -rq` ตรงกันทุกไบต์ (15 ส.ค. 2569) โฟลเดอร์ `results/`, `results_test/`,
`results_150/`, `results_150_test/` ที่ root ของ `01_frame-extractor-tool/` **ไม่ได้ถูกใช้** เพราะ
`results_all`/`results_all_test` เป็นซูเปอร์เซ็ตที่รวมข้อมูลของทั้งสี่โฟลเดอร์นั้นไว้แล้ว (`results`/
`results_test` คือ noaug+aug 50ep รอบเดิม, `results_150`/`results_150_test` คือ aug150 640px —
ไฟล์ในนั้นตรงกับที่อยู่ใน `results_all` ทุกไบต์ ใช้แค่เป็น input ให้โน้ตบุ๊ก `ssid9_aug150_RESULTS.ipynb`)

| ไฟล์เฉพาะของโฟลเดอร์นี้ | แสดงอะไร | สไลด์ |
|---|---|---|
| `benchmark_comparison.md` / `.csv` | **ตารางเทียบสี่รอบตัวหลัก** — ที่มาของกติกา ❶ ทั้งหมด | 19, 21 |
| `hyperparameter_summary.md` / `.csv` | ทุก hyperparameter ของสี่รอบ พร้อมแถว `** DIFFERS **` ที่บอกว่าอะไรต่างกันบ้าง — **หลักฐานว่าคุมตัวแปรจริง** | B1 |
| `comparison_map50_curves.png` | เส้น mAP50 ของสี่รอบซ้อนกัน — เห็น noaug ตันตั้งแต่ต้น | 20 |
| `comparison_map50-95_curves.png` | เส้น mAP50-95 ซ้อนกัน — **ที่นี่ 960 แยกตัวออกจาก 640 ชัดกว่ากราฟบน** | 21 |
| `comparison_loss_curves.png` | loss ของสี่รอบซ้อนกัน | B ตอบคำถาม |
| `comparison_per_class_map50.png` | mAP50 รายคลาสของทั้งสี่รอบ — เห็น "ทุกคลาสดีขึ้น ไม่มีคลาสไหนแย่ลง" | 22 |

ที่เหลืออีก ~85 ไฟล์คือชุดรูปเดียวกับหัวข้อ 2 ครบทั้งสี่รอบ (`noaug_*`, `aug_*`, `aug150_*`, `aug960_*`)
มีไว้ให้เจาะดูรอบใดรอบหนึ่งได้เวลาถูกถาม

---

## 4. `assets/results-boxsize/` — 2 ไฟล์

| ไฟล์ | แสดงอะไร | สไลด์ |
|---|---|---|
| `box_size_by_class.md` / `.csv` | ขนาดกล่องรายคลาส เทียบเส้น small/medium/large ของ COCO · **needle ด้านสั้นมัธยฐาน 12.4 px ที่ imgsz 640 และ 80.9% ของกล่องอยู่ใต้เส้น small** | 27, B4 |

ต้นฉบับอยู่ที่ `01_frame-extractor-tool/results_boxsize/` — ตรวจแล้วด้วย `diff -rq` ตรงกันทุกไบต์
(15 ส.ค. 2569)

นี่คือไฟล์ที่เปลี่ยน "needle คงต้องการข้อมูลเพิ่ม" ให้กลายเป็น "needle เป็นปัญหาขนาดวัตถุ วัดได้"

---

## 5. `assets/screenshots-webapp/` — 17 ภาพ

prefix คือวันที่ถ่าย เพราะสองชุดใช้เลขซ้ำกัน (`01-` ทั้งคู่)

| ไฟล์ | แสดงอะไร |
|---|---|
| `2026-07-30__webapp-extract-form.png` · `2026-07-30__webapp-extract-result.png` | ชุดแรกสุด ตอนมีแค่ Extract |
| `2026-08-03__01-extract-upload.png` · `2026-08-03__02-extract-done.png` · `2026-08-03__03-detect-done.png` · `2026-08-03__04-annotate-boxes.png` · `2026-08-03__05-export-stats.png` · `2026-08-03__06-export-done.png` | 6 ภาพครบ flow: อัปโหลด → สกัดเสร็จ → ตรวจจับ → กำกับกล่อง → สถิติ export → export เสร็จ |
| `2026-08-12__01-login.jpg` | หน้าล็อกอิน (ระบบมีรหัสผ่านแล้ว) |
| `2026-08-12__02-home-extract-tab.jpg` | หน้าแรก แท็บ Extract |
| `2026-08-12__06-detect-model-picker.jpg` | ตัวเลือกโมเดล — เห็นว่าเลือกโมเดลบนเซิร์ฟเวอร์ได้ |
| `2026-08-12__07-model-loaded-ready.jpg` | สถานะโมเดลพร้อมใช้ พร้อมจำนวนคลาส |
| `2026-08-12__10-annotate-overview.jpg` | หน้ากำกับข้อมูล |
| `2026-08-12__11-annotate-polygon-tool.jpg` | เครื่องมือ polygon |
| `2026-08-12__12-export-format.jpg` · `2026-08-12__13-export-split-augment.jpg` | ตัวเลือกรูปแบบ export และการแบ่ง split |
| `2026-08-12__14-analytics.jpg` | หน้า Analytics |

**ที่ยังไม่มี:** 15 จาก 24 ภาพที่วางแผนไว้ ที่ขาดเด่น ๆ คือหน้า Detect ระหว่างทำงาน, OCR, และ logout
— ถ้าสไลด์ต้องการภาพเหล่านี้ ต้องรันเว็บแอปถ่ายเพิ่ม

---

## 6. `source-docs/` — 9 ไฟล์ + โน้ตบุ๊ก 3 เล่ม (ข้อความต้นทาง ไม่ใช่รูป)

| ไฟล์ | คืออะไร | ใช้ทำอะไรกับสไลด์ |
|---|---|---|
| `abstract-2026-08-12.md` | บทคัดย่อไทย+อังกฤษ ฉบับที่ตรวจตัวเลขแล้ว | ที่มาของสไลด์ 3, 11 · ใช้ยกข้อความได้ตรง ๆ |
| `model-results-2026-08-13.md` | **รายงานผลโมเดลฉบับเต็ม** รวมย่อหน้าแก้ไขเรื่อง imgsz | ที่มาหลักของบทที่ 5 ทั้งบท |
| `models-README.md` | สเปกของ checkpoint ทุกตัว พร้อมข้อควรระวังรายตัว | B11, กติกา ❸ ❼ ❽ |
| `edit-checklist-2026-08-12.md` | รายการแก้รายงาน — **บอกว่าอะไรเคยเขียนผิดและแก้เป็นอะไร** | กติกา ❹ · กันไม่ให้ข้อความที่ถูกตัดไปแล้วกลับเข้าสไลด์ |
| `ch3-replacement-p30-32.md` | เนื้อหาบทที่ 3 ฉบับเขียนใหม่ | บทที่ 3-4 ของสไลด์ |
| `WORK_PLAN.md` | แผนงานเว็บแอป 944 บรรทัด รวมเหตุผลการตัดสินใจทุกข้อ | **ที่มาของคำตอบเวลาอาจารย์ถามว่า "ทำไมเลือกแบบนี้"** เช่น ทำไมไม่ใช้ DB, ทำไมไม่ใช้ Docker |
| `Diagrams-A-model.html` | แคตตาล็อกรูปชุด ก พร้อมคำอธิบาย "ตอบคำถามอะไร" ของแต่ละรูป | **อ่านก่อนเขียนบทพูด** — มีคำอธิบายเชิงวิชาการของทุกรูป |
| `Diagrams-B-webapp.html` | แคตตาล็อกรูปชุด ข เหมือนกัน | เดียวกัน |
| `Webapp-review.html` | หน้ารีวิวเว็บแอป 9 ภาพหน้าจอ | ดูภาพหน้าจอพร้อมคำอธิบายในหน้าเดียว |

### `source-docs/notebooks/` — 3 เล่ม เปิดอ่านได้เลย ไม่ต้องรัน

โน้ตบุ๊กฝังรูปและเอาต์พุตไว้ในไฟล์แล้ว **มีประโยชน์ตอนอาจารย์ขอดูว่ารันจริงไหม**

| ไฟล์ | คืออะไร |
|---|---|
| `ssid9_augmentation_ab_RESULTS.ipynb` | การทดลอง A/B ต้นเรื่อง (noaug vs aug, 50 epochs) |
| `ssid9_aug150_RESULTS.ipynb` | รอบ 150 epochs ที่ imgsz 640 |
| `ssid9_aug960_RESULTS.ipynb` | รอบ 150 epochs ที่ imgsz 960 — **โมเดลที่ดีที่สุด** |

### สิ่งที่ตั้งใจ "ไม่" ใส่ในแพ็ก และเหตุผล

| ไฟล์ | ทำไมถึงตัดออก |
|---|---|
| `Diagrams-11-figures.html` · `Diagrams-redraw-guide.html` | แคตตาล็อกรูปชุดเดิม **ถูกแทนที่ด้วยชุด ก/ข แล้ว** ถ้าเอาเข้ามาจะมีรูปสองชุดที่ขัดกัน เสี่ยงหยิบรูปเก่าไปทำสไลด์ (ยังอยู่ที่ `05_reports/`) |
| `Report-WebApp-Current-State.md` | เขียนไว้ 30 ก.ค. ตอนเว็บแอปมีแค่ Extract — **ล้าสมัยจนอันตราย** ถ้าใครหยิบไปทำสไลด์จะได้สถานะที่ผิด สถานะจริงอยู่ใน diagram ชุด ข + ภาพหน้าจอ |
| `changelog-docx-2026-08-12.md` | บันทึกการแก้ไฟล์ Word ภายใน ไม่ใช่เนื้อหาโครงงาน |
| `walkthrough.md` · `task.md` · `implementation_plan.md` (ที่ราก repo) | เป็นเรื่อง scroll animation ของหน้า landing page และการจัดโฟลเดอร์ ไม่เกี่ยวกับโครงงานที่นำเสนอ |
| `02_web-app/` (landing page) | คนละงานกับเว็บแอปกำกับข้อมูลที่นำเสนอ |
| ไฟล์โมเดล `.pt` | ไม่ใช่วัตถุดิบสไลด์ · อยู่ที่ `01_frame-extractor-tool/models/` ถ้าต้อง demo |

---

## 7. ไฟล์ที่มีอยู่ก่อนแล้วในโฟลเดอร์นี้ (ไม่ได้แตะ)

| ไฟล์ | หมายเหตุ |
|---|---|
| `Report-Phase-1-1 (1).pdf` | รายงานของอาจารย์ ฉบับตั้งต้นของโครงงาน |
| `ระบบตรวจจับ...-66112772-66120361-66126467.pdf` | รายงานฉบับของทีม |
| `Video ทดสอบ 30 วิ.mp4` | วิดีโอทดสอบ 30 วินาที (81 MB) — ใช้เป็น demo ตอนนำเสนอได้ |

---

## หมายเหตุการใช้งาน

- โฟลเดอร์นี้ชื่อมีเว้นวรรค (`Design systems`) — เวลาเขียนสคริปต์ต้องใส่เครื่องหมายคำพูดครอบ path เสมอ
- ขนาดรวมของแพ็ก ~125 MB (ส่วนใหญ่คือวิดีโอ 81 MB)
- ถ้าต้นฉบับใน `05_reports/` หรือ `results_*/` ถูกอัปเดต แพ็กนี้ **ไม่อัปเดตตาม** ต้องคัดลอกใหม่
